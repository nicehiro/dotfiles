# bin

Utility scripts.

## Scripts

### `auto-commit.sh`

Automated git commit and push. Stages all changes, commits with a timestamped message, and pushes to the current branch.

```bash
./bin/auto-commit.sh /path/to/repo
```
